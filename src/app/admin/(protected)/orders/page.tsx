import Link from "next/link";
import { adminOrderQuerySchema } from "@/features/admin/order-schema";
import { formatOrderTime, orderStatusLabels } from "@/features/order/presentation";
import { formatCny } from "@/lib/money";
import { getAdminSession } from "@/server/admin/auth";
import { adminOrderService } from "@/server/admin-orders";
export default async function AdminOrdersPage(props: PageProps<"/admin/orders">) { const query = adminOrderQuerySchema.parse(await props.searchParams); const admin = await getAdminSession(); const result = await adminOrderService.list(admin, query); if (!result.ok) return null; const pages = Math.max(1, Math.ceil(result.data.total / 10)); const href = (page: number) => { const p = new URLSearchParams(); if (query.search) p.set("search", query.search); if (query.status) p.set("status", query.status); p.set("page", String(page)); return `/admin/orders?${p}`; }; return <main><p className="text-sm tracking-[0.2em] text-amber-800">交易履约</p><h1 className="mt-2 text-3xl font-semibold">订单管理</h1><form className="mt-6 flex flex-wrap gap-3 rounded-2xl bg-white p-4"><input name="search" defaultValue={query.search} placeholder="订单号、用户名称或邮箱" className="min-w-64 flex-1 rounded-xl border border-stone-300 px-4 py-2" /><select name="status" defaultValue={query.status ?? ""} className="rounded-xl border border-stone-300 px-4 py-2"><option value="">全部状态</option>{Object.entries(orderStatusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><button className="rounded-full bg-stone-900 px-5 py-2 text-white">筛选</button></form><div className="mt-6 space-y-3">{result.data.items.map((order) => <Link key={order.id} href={`/admin/orders/${order.orderNo}`} className="grid gap-3 rounded-3xl border border-stone-200 bg-white p-5 transition hover:border-amber-300 md:grid-cols-[1fr_1fr_auto_auto] md:items-center"><div><p className="font-medium">{order.orderNo}</p><p className="text-xs text-stone-500">{formatOrderTime(order.createdAt)}</p></div><div><p className="text-sm">{order.userName}</p><p className="text-xs text-stone-500">{order.userEmail}</p></div><span className="rounded-full bg-stone-100 px-3 py-1 text-center text-sm">{orderStatusLabels[order.status]}</span><strong className="text-right text-amber-800">{formatCny(order.totalCents)}</strong></Link>)}</div>{result.data.items.length === 0 ? <p className="mt-6 rounded-3xl border border-dashed border-stone-300 p-12 text-center text-stone-500">没有符合条件的订单</p> : null}<div className="mt-6 flex justify-end gap-3"><Link href={href(Math.max(1, query.page - 1))} className="rounded-full border border-stone-300 px-4 py-2 text-sm">上一页</Link><span className="py-2 text-sm text-stone-500">{query.page} / {pages}</span><Link href={href(Math.min(pages, query.page + 1))} className="rounded-full border border-stone-300 px-4 py-2 text-sm">下一页</Link></div></main>; }
